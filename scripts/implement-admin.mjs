import fs from 'node:fs';
import path from 'node:path';
const root=process.cwd();
const put=(p,s)=>{const f=path.join(root,p);fs.mkdirSync(path.dirname(f),{recursive:true});fs.writeFileSync(f,s,'utf8')};
const old=fs.readFileSync(path.join(root,'src/data/products.ts'),'utf8');
const marker=old.indexOf('export const products'); const start=old.indexOf('[',old.indexOf('=',marker)), end=old.lastIndexOf(']');
if(start<0||end<start) throw new Error('Products array not found');
const products=JSON.parse(old.slice(start,end+1));
put('data/products.json',JSON.stringify(products,null,2)+'\n');
put('data/order-products.json',JSON.stringify(products,null,2)+'\n');
put('src/data/products.ts',`import source from '../../data/products.json';\n\nexport type Product = {\n  id: number;\n  name: string;\n  price: number;\n  newPrice: number | null;\n  description: string;\n  link: string;\n  images: string[];\n};\n\n// Typed compatibility adapter for existing public consumers.\nexport const products: Product[] = source satisfies Product[];\n`);
put('src/server/config.ts',`import 'server-only';\n\nexport type ServerConfig={adminPassword:string;sessionSecret:string;githubToken:string;owner:string;repo:string;branch:string;productsPath:string;managedPrefix:string};\nexport function getConfig():ServerConfig|null{\n const e=process.env; const required=[e.ADMIN_PASSWORD,e.SESSION_SECRET,e.GITHUB_TOKEN,e.GITHUB_OWNER,e.GITHUB_REPO];\n if(required.some(v=>!v)) return null;\n return {adminPassword:e.ADMIN_PASSWORD!,sessionSecret:e.SESSION_SECRET!,githubToken:e.GITHUB_TOKEN!,owner:e.GITHUB_OWNER!,repo:e.GITHUB_REPO!,branch:e.GITHUB_BRANCH||'main',productsPath:e.GITHUB_PRODUCTS_PATH||'data/products.json',managedPrefix:'public/products/admin/'};\n}\n`);
put('src/server/security.ts',`import 'server-only';\nimport {createHmac,randomBytes,timingSafeEqual,createHash} from 'node:crypto';\nimport {cookies,headers} from 'next/headers';\nimport {NextRequest,NextResponse} from 'next/server';\nimport {getConfig} from './config';\nconst COOKIE='vibe_admin_session', HOURS=8;\ntype Session={exp:number;csrf:string};\nconst b64=(v:Buffer|string)=>Buffer.from(v).toString('base64url');\nconst sign=(v:string,s:string)=>b64(createHmac('sha256',s).update(v).digest());\nexport function safeEqual(a:string,b:string){const x=createHash('sha256').update(a).digest(),y=createHash('sha256').update(b).digest();return timingSafeEqual(x,y)}\nexport function newSession(secret:string){const value=b64(JSON.stringify({exp:Date.now()+HOURS*3600_000,csrf:b64(randomBytes(24))}));return {token:value+'.'+sign(value,secret),data:JSON.parse(Buffer.from(value,'base64url').toString()) as Session}}\nexport function decodeSession(token:string|undefined,secret:string){if(!token)return null;const [v,s]=token.split('.');if(!v||!s||!safeEqual(s,sign(v,secret)))return null;try{const d=JSON.parse(Buffer.from(v,'base64url').toString()) as Session;return d.exp>Date.now()?d:null}catch{return null}}\nexport async function currentSession(){const c=getConfig();if(!c)return null;return decodeSession((await cookies()).get(COOKIE)?.value,c.sessionSecret)}\nexport function setSession(res:NextResponse,token:string){res.cookies.set(COOKIE,token,{httpOnly:true,sameSite:'strict',secure:process.env.NODE_ENV==='production',path:'/',maxAge:HOURS*3600})}\nexport function clearSession(res:NextResponse){res.cookies.set(COOKIE,'',{httpOnly:true,sameSite:'strict',secure:process.env.NODE_ENV==='production',path:'/',maxAge:0})}\nconst attempts=new Map<string,{n:number;until:number}>();\nexport function throttle(key:string,limit=8,windowMs=60_000){const now=Date.now(),v=attempts.get(key);if(!v||v.until<now){attempts.set(key,{n:1,until:now+windowMs});return false}v.n++;return v.n>limit}\nexport function clientKey(req:NextRequest){return req.headers.get('x-forwarded-for')?.split(',')[0]?.trim()||'unknown'}\nexport async function protectMutation(req:NextRequest){const cfg=getConfig();if(!cfg)return 'Сервер не настроен';const origin=req.headers.get('origin');const host=req.headers.get('host');if(!origin||new URL(origin).host!==host)return 'Запрос с другого источника запрещён';const session=decodeSession(req.cookies.get(COOKIE)?.value,cfg.sessionSecret);if(!session)return 'Требуется вход';if(!safeEqual(req.headers.get('x-csrf-token')||'',session.csrf))return 'Неверный CSRF-токен';return null}\nexport const noStore={'Cache-Control':'no-store, max-age=0','Pragma':'no-cache'};\n`);
put('src/server/products-validation.ts',`import 'server-only';\nimport type {Product} from '@/data/products';\nexport const MAX_FILES=8,MAX_FILE=3145728,MAX_TOTAL=4194304;\nexport function validateProduct(v:unknown):Product{if(!v||typeof v!=='object')throw new Error('Неверные данные');const x=v as Record<string,unknown>;const id=Number(x.id),price=Number(x.price),np=x.newPrice===null||x.newPrice===''?null:Number(x.newPrice);if(!Number.isSafeInteger(id)||id<=0)throw new Error('ID должен быть положительным целым числом');if(typeof x.name!=='string'||!x.name.trim()||x.name.length>160)throw new Error('Проверьте название');if(!Number.isFinite(price)||price<0||price>1e9||np!==null&&(!Number.isFinite(np)||np<0||np>1e9))throw new Error('Проверьте цену');if(typeof x.description!=='string'||x.description.length>10000)throw new Error('Описание слишком длинное');if(typeof x.link!=='string'||x.link.length>2048)throw new Error('Ссылка слишком длинная');if(x.link&&!(x.link.startsWith('/')||/^https:\/\//.test(x.link)))throw new Error('Недопустимая ссылка');if(!Array.isArray(x.images)||x.images.some(i=>typeof i!=='string'||i.length>2048||(!i.startsWith('/')&&!/^https:\/\//.test(i))))throw new Error('Недопустимые изображения');return {id,name:x.name.trim(),price,newPrice:np,description:x.description,link:x.link,images:x.images as string[]}}\nconst signatures=[['image/jpeg',[0xff,0xd8,0xff]],['image/png',[0x89,0x50,0x4e,0x47]],['image/webp',[0x52,0x49,0x46,0x46]]] as const;\nexport async function validateFiles(files:File[]){if(files.length>MAX_FILES)throw new Error('Максимум 8 файлов');if(files.reduce((n,f)=>n+f.size,0)>MAX_TOTAL)throw new Error('Общий размер больше 4 МБ');return Promise.all(files.map(async(f,i)=>{if(f.size>MAX_FILE)throw new Error('Файл больше 3 МБ');const bytes=new Uint8Array(await f.arrayBuffer());const hit=signatures.find(([mime,s])=>f.type===mime&&s.every((b,j)=>bytes[j]===b));if(!hit||(f.type==='image/webp'&&String.fromCharCode(...bytes.slice(8,12))!=='WEBP'))throw new Error('Разрешены только настоящие JPG, PNG и WEBP');const ext=f.type==='image/jpeg'?'jpg':f.type.split('/')[1];return {bytes,path:'public/products/admin/'+Date.now()+'-'+i+'-'+crypto.randomUUID()+'.'+ext,url:'/'+('products/admin/'+Date.now()+'-'+i+'-'+crypto.randomUUID()+'.'+ext)}}))}\n`);
put('src/server/github.ts',`import 'server-only';\nimport {getConfig} from './config';import type {Product} from '@/data/products';\ntype Change={path:string;content?:Uint8Array|string;delete?:boolean};\nasync function gh(path:string,init:RequestInit={}){const c=getConfig();if(!c)throw Object.assign(new Error('GitHub не настроен'),{status:503});const r=await fetch('https://api.github.com/repos/'+c.owner+'/'+c.repo+path,{...init,headers:{Accept:'application/vnd.github+json',Authorization:'Bearer '+c.githubToken,'X-GitHub-Api-Version':'2022-11-28',...init.headers},cache:'no-store'});if(!r.ok)throw Object.assign(new Error(r.status===409?'Конфликт версии GitHub':'Ошибка GitHub: '+r.status),{status:r.status});return r.json()}\nexport async function readProducts(){const c=getConfig();if(!c)throw Object.assign(new Error('GitHub не настроен'),{status:503});const ref=await gh('/git/ref/heads/'+encodeURIComponent(c.branch));const revision=ref.object.sha as string;const file=await gh('/contents/'+c.productsPath+'?ref='+encodeURIComponent(c.branch));const text=Buffer.from(file.content.replace(/\\n/g,''),'base64').toString('utf8');return {products:JSON.parse(text) as Product[],revision}}\nexport async function atomicCommit(expected:string,products:Product[],extra:Change[],message:string){const c=getConfig()!;const ref=await gh('/git/ref/heads/'+encodeURIComponent(c.branch));if(ref.object.sha!==expected)throw Object.assign(new Error('Каталог изменён. Обновите страницу.'),{status:409});const parent=await gh('/git/commits/'+expected);const changes:Change[]=[{path:c.productsPath,content:JSON.stringify(products,null,2)+'\\n'},...extra];const tree=[];for(const ch of changes){if(ch.delete){tree.push({path:ch.path,mode:'100644',type:'blob',sha:null});continue}const blob=await gh('/git/blobs',{method:'POST',body:JSON.stringify({content:Buffer.from(ch.content!).toString('base64'),encoding:'base64'}),headers:{'Content-Type':'application/json'}});tree.push({path:ch.path,mode:'100644',type:'blob',sha:blob.sha})}const t=await gh('/git/trees',{method:'POST',body:JSON.stringify({base_tree:parent.tree.sha,tree}),headers:{'Content-Type':'application/json'}});const commit=await gh('/git/commits',{method:'POST',body:JSON.stringify({message,tree:t.sha,parents:[expected]}),headers:{'Content-Type':'application/json'}});await gh('/git/refs/heads/'+encodeURIComponent(c.branch),{method:'PATCH',body:JSON.stringify({sha:commit.sha,force:false}),headers:{'Content-Type':'application/json'}});return commit.sha as string}\n`);
put('src/app/api/admin/auth/login/route.ts',`import {NextRequest,NextResponse} from 'next/server';import {getConfig} from '@/server/config';import {clientKey,newSession,noStore,safeEqual,setSession,throttle} from '@/server/security';\nexport const runtime='nodejs';export async function POST(req:NextRequest){const key=clientKey(req);if(throttle('login:'+key,6,300000))return NextResponse.json({error:'Слишком много попыток'},{status:429,headers:noStore});const c=getConfig();if(!c)return NextResponse.json({error:'Админ-доступ не настроен'},{status:503,headers:noStore});const {password}=await req.json().catch(()=>({}));if(typeof password!=='string'||!safeEqual(password,c.adminPassword))return NextResponse.json({error:'Неверный пароль'},{status:401,headers:noStore});const {token,data}=newSession(c.sessionSecret),res=NextResponse.json({authenticated:true,csrf:data.csrf},{headers:noStore});setSession(res,token);return res}\n`);
put('src/app/api/admin/auth/status/route.ts',`import {NextResponse} from 'next/server';import {currentSession,noStore} from '@/server/security';import {getConfig} from '@/server/config';export const runtime='nodejs';export async function GET(){const s=await currentSession();return NextResponse.json({authenticated:!!s,configured:!!getConfig(),csrf:s?.csrf},{headers:noStore})}\n`);
put('src/app/api/admin/auth/logout/route.ts',`import {NextRequest,NextResponse} from 'next/server';import {clearSession,noStore,protectMutation} from '@/server/security';export const runtime='nodejs';export async function POST(r:NextRequest){const e=await protectMutation(r);if(e)return NextResponse.json({error:e},{status:403,headers:noStore});const res=NextResponse.json({ok:true},{headers:noStore});clearSession(res);return res}\n`);
put('src/app/api/admin/products/route.ts',`import {NextRequest,NextResponse} from 'next/server';import {noStore,protectMutation,throttle,clientKey,currentSession} from '@/server/security';import {readProducts,atomicCommit} from '@/server/github';import {validateFiles,validateProduct} from '@/server/products-validation';import {getConfig} from '@/server/config';export const runtime='nodejs';export const dynamic='force-dynamic';const out=(x:unknown,s=200)=>NextResponse.json(x,{status:s,headers:noStore});function fail(e:unknown){const x=e as Error&{status?:number};return out({error:x.message||'Ошибка'},x.status||400)}\nexport async function GET(){if(!await currentSession())return out({error:'Требуется вход'},401);try{return out(await readProducts())}catch(e){return fail(e)}}\nexport async function POST(r:NextRequest){return mutate(r,'create')}export async function PATCH(r:NextRequest){return mutate(r,'update')}export async function DELETE(r:NextRequest){return mutate(r,'delete')}\nasync function mutate(r:NextRequest,op:'create'|'update'|'delete'){const denied=await protectMutation(r);if(denied)return out({error:denied},403);if(throttle('mut:'+clientKey(r),30,60000))return out({error:'Слишком много запросов'},429);try{const form=await r.formData(),expected=String(form.get('revision')||''),raw=JSON.parse(String(form.get('product')||'{}')),files=form.getAll('images').filter(x=>x instanceof File) as File[];if(!/^[0-9a-f]{40}$/.test(expected))throw new Error('Неверная ревизия');const current=await readProducts(),cfg=getConfig()!;if(current.revision!==expected)return out({error:'Каталог изменён. Обновите страницу.'},409);let list=[...current.products],extra:{path:string;content?:Uint8Array;delete?:boolean}[]=[];if(op==='delete'){const id=Number(raw.id);if(!Number.isSafeInteger(id))throw new Error('Неверный ID');if(!list.some(p=>p.id===id))throw new Error('Товар не найден');const removed=list.find(p=>p.id===id)!;list=list.filter(p=>p.id!==id);const used=new Set(list.flatMap(p=>p.images));for(const url of removed.images)if(url.startsWith('/products/admin/')&&!used.has(url)){const p='public'+url;if(p.startsWith(cfg.managedPrefix)&&!p.includes('..'))extra.push({path:p,delete:true})}}else{const product=validateProduct(raw);const exists=list.some(p=>p.id===product.id);if(op==='create'&&exists)throw new Error('ID уже существует');if(op==='update'&&!exists)throw new Error('Товар не найден');const uploads=await validateFiles(files);for(const u of uploads){product.images.push(u.url);extra.push({path:u.path,content:u.bytes})}list=op==='create'?[...list,product]:list.map(p=>p.id===product.id?product:p)}list.sort((a,b)=>a.id-b.id);const revision=await atomicCommit(expected,list,extra,'admin: '+op+' product '+raw.id);return out({ok:true,revision})}catch(e){return fail(e)}}\n`);
put('src/app/admin/layout.tsx',`export const metadata={title:'Управление каталогом'};export default function AdminLayout({children}:{children:React.ReactNode}){return <>{children}</>}\n`);
put('src/app/admin/page.tsx',`'use client';import {useEffect,useMemo,useState} from 'react';import type {Product} from '@/data/products';import './admin.css';type State={authenticated:boolean;configured:boolean;csrf?:string};const blank:Product={id:0,name:'',price:0,newPrice:null,description:'',link:'',images:[]};export default function Admin(){const [auth,setAuth]=useState<State|null>(null),[password,setPassword]=useState(''),[items,setItems]=useState<Product[]>([]),[revision,setRevision]=useState(''),[query,setQuery]=useState(''),[edit,setEdit]=useState<Product|null>(null),[files,setFiles]=useState<File[]>([]),[msg,setMsg]=useState('');const request=async(url:string,init?:RequestInit)=>{const r=await fetch(url,{...init,cache:'no-store'}),j=await r.json();if(!r.ok)throw new Error(j.error||'Ошибка');return j};const load=async()=>{try{const j=await request('/api/admin/products');setItems(j.products);setRevision(j.revision)}catch(e){setMsg((e as Error).message)}};useEffect(()=>{request('/api/admin/auth/status').then((s:State)=>{setAuth(s);if(s.authenticated)load()}).catch(()=>setAuth({authenticated:false,configured:false}))},[]);const login=async(e:React.FormEvent)=>{e.preventDefault();try{const s=await request('/api/admin/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({password})});setAuth({authenticated:true,configured:true,csrf:s.csrf});setPassword('');await load()}catch(e){setMsg((e as Error).message)}};const save=async(e:React.FormEvent)=>{e.preventDefault();if(!edit||!auth?.csrf)return;setMsg('Сохранение…');try{const fd=new FormData();fd.set('revision',revision);fd.set('product',JSON.stringify(edit));files.forEach(f=>fd.append('images',f));await request('/api/admin/products',{method:items.some(x=>x.id===edit.id)?'PATCH':'POST',headers:{'x-csrf-token':auth.csrf},body:fd});setEdit(null);setFiles([]);setMsg('Сохранено');await load()}catch(e){setMsg((e as Error).message)}};const remove=async(p:Product)=>{if(!auth?.csrf||!confirm('Удалить «'+p.name+'»? Это действие попадёт в GitHub.'))return;try{const fd=new FormData();fd.set('revision',revision);fd.set('product',JSON.stringify({id:p.id}));await request('/api/admin/products',{method:'DELETE',headers:{'x-csrf-token':auth.csrf},body:fd});setMsg('Удалено');await load()}catch(e){setMsg((e as Error).message)}};const shown=useMemo(()=>items.filter(p=>(p.name+' '+p.id).toLowerCase().includes(query.toLowerCase())),[items,query]);if(auth===null)return <main className="admin"><p>Проверка доступа…</p></main>;if(!auth.authenticated)return <main className="admin login"><form className="panel" onSubmit={login}><img src="/logo.jpeg" alt="VIBE AZ"/><h1>Управление каталогом</h1>{!auth.configured&&<p className="error">Сервер не настроен. Проверьте переменные окружения.</p>}<label>Пароль<input type="password" value={password} onChange={e=>setPassword(e.target.value)} autoComplete="current-password" required/></label><button>Войти</button>{msg&&<p className="error">{msg}</p>}</form></main>;return <main className="admin"><header><div><h1>Каталог</h1><p>{items.length} товаров · ревизия {revision.slice(0,7)}</p></div><button className="secondary" onClick={async()=>{await request('/api/admin/auth/logout',{method:'POST',headers:{'x-csrf-token':auth.csrf||''}});location.reload()}}>Выйти</button></header><section className="toolbar"><input placeholder="Поиск по названию или ID" value={query} onChange={e=>setQuery(e.target.value)}/><button onClick={()=>setEdit({...blank,id:Math.max(0,...items.map(x=>x.id))+1})}>Добавить товар</button></section>{msg&&<p className="status">{msg}</p>}<section className="grid">{shown.map(p=><article className="card" key={p.id}><img src={p.images[0]||'/logo.jpeg'} alt=""/><div><b>#{p.id} · {p.name}</b><p>{p.newPrice??p.price} ₼</p><button className="secondary" onClick={()=>setEdit({...p,images:[...p.images]})}>Изменить</button> <button className="danger" onClick={()=>remove(p)}>Удалить</button></div></article>)}</section>{edit&&<div className="modal" role="dialog"><form className="panel editor" onSubmit={save}><h2>{items.some(x=>x.id===edit.id)?'Редактирование':'Новый товар'}</h2><div className="fields"><label>ID<input type="number" min="1" value={edit.id} disabled={items.some(x=>x.id===edit.id)} onChange={e=>setEdit({...edit,id:Number(e.target.value)})}/></label><label>Название<input value={edit.name} maxLength={160} required onChange={e=>setEdit({...edit,name:e.target.value})}/></label><label>Цена<input type="number" min="0" step="0.01" value={edit.price} onChange={e=>setEdit({...edit,price:Number(e.target.value)})}/></label><label>Новая цена<input type="number" min="0" step="0.01" value={edit.newPrice??''} onChange={e=>setEdit({...edit,newPrice:e.target.value===''?null:Number(e.target.value)})}/></label><label className="wide">Описание<textarea value={edit.description} onChange={e=>setEdit({...edit,description:e.target.value})}/></label><label className="wide">Ссылка<input value={edit.link} onChange={e=>setEdit({...edit,link:e.target.value})}/></label><label className="wide">Изображения (до 8; JPG, PNG, WEBP)<input type="file" accept="image/jpeg,image/png,image/webp" multiple onChange={e=>setFiles(Array.from(e.target.files||[]))}/></label></div><div className="previews">{edit.images.map((x,i)=><span key={x}><img src={x} alt=""/><button type="button" onClick={()=>setEdit({...edit,images:edit.images.filter((_,j)=>i!==j)})}>×</button></span>)}{files.map(f=><img key={f.name} src={URL.createObjectURL(f)} alt="preview"/>)}</div><footer><button type="button" className="secondary" onClick={()=>{setEdit(null);setFiles([])}}>Отмена</button><button>Сохранить</button></footer></form></div>}</main>}\n`);
put('src/app/admin/admin.css',`.admin{min-height:100vh;background:#f4f6f8;color:#15171a;padding:24px;font-family:Arial,sans-serif}.admin *{box-sizing:border-box}.admin header,.toolbar{max-width:1100px;margin:0 auto 20px;display:flex;align-items:center;justify-content:space-between;gap:16px}.admin h1,.admin p{margin:4px 0}.admin button{border:0;border-radius:10px;background:#ff6b35;color:#fff;padding:11px 16px;font-weight:700;cursor:pointer}.admin .secondary{background:#e5e8eb;color:#222}.admin .danger{background:#b42318}.toolbar input,.admin input,.admin textarea{width:100%;border:1px solid #ccd1d6;border-radius:9px;padding:11px;background:#fff;color:#111}.toolbar input{max-width:600px}.grid{max-width:1100px;margin:auto;display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px}.card{display:flex;gap:14px;padding:12px;background:#fff;border-radius:14px;box-shadow:0 2px 12px #0000000d}.card>img{width:92px;height:92px;object-fit:cover;border-radius:10px}.login{display:grid;place-items:center}.panel{background:white;padding:24px;border-radius:16px;box-shadow:0 12px 40px #0002;width:min(100%,420px)}.login img{width:72px;height:72px;object-fit:contain}.panel label{display:grid;gap:6px;margin:12px 0}.error{color:#b42318}.status{max-width:1100px;margin:0 auto 14px}.modal{position:fixed;inset:0;background:#0008;display:grid;place-items:center;padding:16px;z-index:9999}.editor{width:min(850px,100%);max-height:92vh;overflow:auto}.fields{display:grid;grid-template-columns:1fr 2fr;gap:0 14px}.wide{grid-column:1/-1}.admin textarea{min-height:120px;resize:vertical}.previews{display:flex;gap:8px;flex-wrap:wrap;margin:14px 0}.previews img{width:80px;height:80px;object-fit:cover;border-radius:8px}.previews span{position:relative}.previews span button{position:absolute;right:-5px;top:-5px;padding:2px 7px;border-radius:50%;background:#222}.editor footer{display:flex;justify-content:flex-end;gap:10px}@media(max-width:700px){.admin{padding:14px}.grid{grid-template-columns:1fr}.toolbar{align-items:stretch;flex-direction:column}.toolbar input{max-width:none}.fields{grid-template-columns:1fr}.wide{grid-column:auto}.card>img{width:72px;height:72px}}`);
// Make the existing shell disappear only for the hidden admin route.
let shell=fs.readFileSync(path.join(root,'src/components/shell.tsx'),'utf8');
if(!shell.includes("usePathname")){shell=shell.replace("import {useState}","import {useState}").replace("from 'next/navigation'","from 'next/navigation'"); if(!shell.includes("from 'next/navigation'")) shell="import {usePathname} from 'next/navigation';"+shell; shell=shell.replace(/export function Shell\(([^)]*)\)\{/,m=>m+"const pathname=usePathname();if(pathname.startsWith('/admin'))return <>{children}</>;");}
put('src/components/shell.tsx',shell);
const pkg=JSON.parse(fs.readFileSync(path.join(root,'package.json'),'utf8'));pkg.scripts.start='next start';delete pkg.scripts.preview;delete pkg.dependencies.serve;put('package.json',JSON.stringify(pkg,null,2)+'\n');
let nc=fs.readFileSync(path.join(root,'next.config.ts'),'utf8').replace(/\s*output:\s*['\"]export['\"],?/g,'');put('next.config.ts',nc);
put('.env.example',`# Server-only. Never prefix these with NEXT_PUBLIC_.\nADMIN_PASSWORD=replace-with-a-long-random-password\nSESSION_SECRET=replace-with-at-least-32-random-bytes\nGITHUB_TOKEN=github-fine-grained-token\nGITHUB_OWNER=owner\nGITHUB_REPO=repository\nGITHUB_BRANCH=main\nGITHUB_PRODUCTS_PATH=data/products.json\n`);
let gi='';try{gi=fs.readFileSync(path.join(root,'.gitignore'),'utf8')}catch{};for(const x of ['.env','.env.local','.env.*.local'])if(!gi.split(/\r?\n/).includes(x))gi+='\n'+x;put('.gitignore',gi.trim()+'\n');
put('README.md',`# VIBE AZ\n\nNext.js storefront with a hidden, server-rendered catalog administrator at \`/admin\`. The public design, routes, phone flows and \`/logo.jpeg\` remain unchanged.\n\n## Local setup\n1. Copy \`.env.example\` to \`.env.local\` and set unique secrets. Never commit this file.\n2. Create a fine-grained GitHub token with **Contents: read/write** access only to the catalog repository.\n3. Run \`npm run dev\`; production uses \`npm run build && npm start\`.\n\nThe canonical catalog is \`data/products.json\`; \`src/data/products.ts\` is the typed adapter used by existing pages. \`data/order-products.json\` is an immutable migration snapshot for resolving historical orders and must not be rewritten by admin CRUD. Catalog edits use the Git Data API to create blobs, one tree, one commit and a non-forced ref update. A stale base revision returns HTTP 409. Images are managed only below \`public/products/admin/\`.\n\n## Vercel\nAdd every variable from \`.env.example\` to the Production/Preview environments. Do not use \`NEXT_PUBLIC_\` for secrets. Configure a Vercel Firewall rate-limit rule for \`/api/admin/auth/login\` (recommended: 5 requests per 5 minutes per IP) and a second rule for \`/api/admin/products\` (for example 30/minute/IP). The in-memory limiter is defense-in-depth only and is not reliable across serverless instances. GitHub commits can trigger the normal Vercel deployment so public static consumers receive the updated JSON.\n\n## Security\nThe administrator is intentionally absent from public navigation, but secrecy of the URL is not an access control. Authentication uses a signed, HttpOnly, Secure-in-production, SameSite=Strict 8-hour cookie. Password/signature comparisons are constant-time. Mutations require a valid session, exact same-origin request and CSRF header. Responses are \`no-store\`; GitHub credentials never reach client code. Use a long random admin password and rotate both it and the session secret when access changes.\n`);
console.log('Implemented admin stage for',products.length,'products');
