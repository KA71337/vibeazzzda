'use client';
import Image from 'next/image';import {useRouter} from 'next/navigation';import {useState} from 'react';import {Heart,ShoppingBag,Zap} from 'lucide-react';import {Product} from '@/data/products';import {useStore} from './store';
export function ProductDetail({p}:{p:Product}){const[img,setImg]=useState(0),{add,toggleFav,favorites}=useStore(),router=useRouter(),fav=favorites.includes(p.id);return <section className="container py-10"><div className="grid gap-10 lg:grid-cols-2"><div><div className="relative aspect-square overflow-hidden rounded-[2rem] bg-gray-100"><Image src={p.images[img]} alt={p.name} fill priority className="object-cover"/></div>{p.images.length>1&&<div className="mt-4 flex gap-3 overflow-x-auto pb-2">{p.images.map((x,i)=><button key={x} onClick={()=>setImg(i)} className={`relative h-20 w-20 shrink-0 overflow-hidden rounded-xl ${i===img?'ring-2 ring-black':''}`}><Image src={x} alt="" fill className="object-cover"/></button>)}</div>}</div><div className="flex flex-col justify-center"><span className="text-xs font-bold uppercase tracking-widest text-gray-500">VIBE AZ · Məhsul #{p.id}</span><h1 className="mt-4 text-4xl font-black sm:text-6xl">{p.name}</h1><p className="mt-6 text-3xl font-black">{p.price} AZN</p><p className="mt-7 whitespace-pre-line leading-7 text-gray-600">{p.description}</p><div className="mt-8 grid gap-3 sm:grid-cols-2"><button onClick={()=>add(p)} className="btn btn-dark"><ShoppingBag size={19}/>Səbətə əlavə et</button><button onClick={()=>{add(p);router.push('/cart')}} className="btn btn-light"><Zap size={19}/>İndi seç</button></div><button onClick={()=>toggleFav(p.id)} className="btn btn-light mt-3"><Heart size={19} fill={fav?'currentColor':'none'}/>{fav?'Seçilmişlərdən sil':'Seçilmişlərə əlavə et'}</button></div></div></section>}
