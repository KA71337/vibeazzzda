import type {Metadata} from 'next';import {notFound} from 'next/navigation';import {products} from '@/data/products';import {ProductDetail} from '@/components/product-detail';
export function generateStaticParams(){return products.map(p=>({id:String(p.id)}))}
export async function generateMetadata({params}:{params:Promise<{id:string}>}):Promise<Metadata>{const{id}=await params,p=products.find(x=>x.id===+id);if(!p)return {};const description=p.description.slice(0,160);return {title:p.name,description,alternates:{canonical:`/product/${p.id}/`},openGraph:{type:'website',url:`/product/${p.id}/`,title:p.name,description,images:p.images[0]?[{url:p.images[0],alt:p.name}]:[{url:'/logo.jpeg',alt:'VIBE AZ'}]},twitter:{card:'summary_large_image',title:p.name,description,images:[p.images[0]||'/logo.jpeg']}}}
export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params,p=products.find(x=>x.id===+id);if(!p)return notFound();const jsonLd={'@context':'https://schema.org','@type':'Product',name:p.name,description:p.description,image:p.images.map(x=>new URL(x,'https://vibe.az').toString()),sku:String(p.id),offers:{'@type':'Offer',priceCurrency:'AZN',price:p.newPrice??p.price,availability:'https://schema.org/InStock',url:`https://vibe.az/product/${p.id}/`}};return <><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(jsonLd).replace(/</g,'\\u003c')}}/><ProductDetail p={p}/></>}
