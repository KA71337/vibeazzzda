'use client';import {products} from '@/data/products';import {ProductCard} from '@/components/product-card';import {useStore} from '@/components/store';import {Heart} from 'lucide-react';export default function Page(){const{favorites,t}=useStore(),list=products.filter(p=>favorites.includes(p.id));return <section className="container py-12"><h1 className="mb-10 text-4xl font-black sm:text-6xl">{t.favorites}</h1>{list.length?<div className="grid grid-cols-2 gap-3 sm:gap-6 lg:grid-cols-4">{list.map(p=><ProductCard key={p.id} p={p}/>)}</div>:<div className="grid place-items-center rounded-3xl bg-gray-50 py-24 text-center"><Heart size={42}/><p className="mt-4 text-gray-500">{t.empty}</p></div>}</section>}
